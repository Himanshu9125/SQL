-- ques=> Get all employee detail from EmployeeDetail table whose "FirstName" not start
-- with any single character between 'a-p'?

SELECT * FROM employee_details
WHERE first_name NOT REGEXP '^[a-pA-P]';

-- oue=>Get all employee detail from EmployeeDetail table whose "Gender" end with 'le'
-- and contain 4 letters. The Underscore(_) Wildcard Character represents any single
-- character.

SELECT * FROM employee_details
WHERE gender LIKE '__le';

